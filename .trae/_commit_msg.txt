release: 升级版本到 1.0.0（首发正式版）

版本号变更：0.2.1 → 1.0.0

已完成的功能（达到 1.0.0 发布标准）：
  1) 菜单项（修改此页参数 / 备份与恢复）固定顺序注册，切换状态用
     Unicode 实心圆 + 文字后缀显示，不再出现顺序混乱
  2) 通知机制从 GM_notification 改页面内 Toast 单例，从根本上
     解决跨 frame 重复通知；加 @noframes + top-frame 守卫
  3) 「修改列表」输入框 ↔ 左侧「当前参数」匹配项联动：
     - smooth 滚动居中
     - 一次性黄底闪烁动画（rAF easeOutCubic + data-flash-key 强制重放）
     - 聚焦常驻蓝框（outline + inset box-shadow）
     - Tab 切换同行 key/value 不丢失蓝框，输入框失焦恢复原样式
  4) 修复聚焦蓝框被滚动容器截断的问题（outline-offset 负值 + inset
     box-shadow + panel padding 双保险）
  5) 架构：构建产物完全从源码仓 master/dist 分支迁出 → 全部走
     GitHub Actions 构建 → 发布到 GitHub Releases assets
     Tampermonkey 通过 master 根目录 meta.user.js 头指针比对 @version
     做自动更新，完整脚本从 Release assets 拉取

变更清单：
  - package.json:         version 0.2.1 → 1.0.0
  - meta.user.js:         重新从构建产物提头，@version=1.0.0
                          @updateURL/@downloadURL 固定指向 master/meta.user.js
